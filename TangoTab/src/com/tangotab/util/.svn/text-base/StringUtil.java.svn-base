package com.tangotab.util;

/**
 * User: Admin
 * Date: 7/7/12
 */
public class StringUtil {

    public static final String GET = "get";
    public static final String SET = "set";

    public static String getGetterMethod(String key) {
        return GET + Character.toUpperCase(key.charAt(0)) + key.substring(1);
    }
    public static String getSetterMethod(String key) {
        return SET + Character.toUpperCase(key.charAt(0)) + key.substring(1);
    }
}
